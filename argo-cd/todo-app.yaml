apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: todo-app
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: asia-east2-docker.pkg.dev/project-60addf72-be9c-4c26-8db/todo-docker-repo
    targetRevision: "99.99.99-latest"
    chart: todo-chart

  destination:
    server: https://kubernetes.default.svc
    namespace: todo-ns
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
      - ApplyOutOfSyncOnly=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
